/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.1.11 leg.glb -t
*/

import * as THREE from "three";
import React, { useRef } from "react";
import { useGLTF, useTexture } from "@react-three/drei";
import { GLTF } from "three-stdlib";

import { Texture } from "../../Utils/Types/Types";
import { Model3D } from "../../Utils/Interfaces/Interfaces";

type GLTFResult = GLTF & {
  nodes: {
    Legs: THREE.Mesh;
  };
  materials: {
    ["Material.001"]: THREE.MeshStandardMaterial;
  };
};

export const Leg: React.FC<Model3D> = ({ texture }) => {
  const { nodes, materials } = useGLTF("Models/leg.glb") as GLTFResult;
  return (
    <group dispose={null}>
      <mesh geometry={nodes.Legs.geometry} position={[0, -0.76, 0]}>
        <meshStandardMaterial map={useTexture(texture)} />
      </mesh>
    </group>
  );
};

useGLTF.preload("Models/leg.glb");
